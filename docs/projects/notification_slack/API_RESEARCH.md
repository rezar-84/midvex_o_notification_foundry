# API Research — Slack

Complete this file before implementation.

## Documentation checked

- URL:
- API version:
- Date checked:
- Required credentials:
- Authentication method:
- Base URL:
- Rate limits:
- Pagination:
- Error format:
- Required headers:

## Endpoints/services to verify

- test connection (`auth.test`);
- send message (`chat.postMessage`);
- webhook/event subscription for inbound linking, if used.

## Implementation note

The channel adapter must implement the foundry adapter contract. Automated tests must use mocked responses and sanitized fixtures, not live API calls.
