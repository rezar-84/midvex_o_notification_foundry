# Changelog — Conversation Foundry

## Unreleased

Nothing built. The module `midvex_o_conversation_foundry` does not exist yet.

## 2026-08-14 — documented

Project created by merging the omnichannel specification pack into this repository. Data model, adapter contract and test plan recorded; naming reconciled to `midvex.conversation.*` (ADR-014) and the adapter contract reconciled onto the existing registry (ADR-015).
